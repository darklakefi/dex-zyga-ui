# Frontend Environment Variables

# Network Configuration
# Options: 'mainnet' or 'devnet'
NEXT_PUBLIC_NETWORK=mainnet

# Helius API Key (REQUIRED for RPC connections)
# Get your free API key at https://www.helius.dev/
# Frontend constructs: https://devnet.helius-rpc.com/?api-key=YOUR_KEY
# or https://mainnet.helius-rpc.com/?api-key=YOUR_KEY
NEXT_PUBLIC_HELIUS_API_KEY=your_helius_api_key_here

# Custom RPC Endpoint (optional, overrides default Helius endpoint)
# If not set, uses Helius RPC for both mainnet and devnet
# NEXT_PUBLIC_RPC_ENDPOINT=your_custom_rpc_endpoint

# Proof Generator API URL
# For local development: http://localhost:4000
# For production (Docker): https://rejewski.vitorpy.com/api (or your domain)
# Note: Frontend runs in browser, so this must be an externally accessible URL
NEXT_PUBLIC_PROOF_API_URL=http://localhost:4000
